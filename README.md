# AI Engineer Pro v4 (Ready to Run)

Local, ready-to-run project using only the Python standard library.

## Run
```bash
python main.py
```

## Features
- Tkinter GUI
- Parallel agents
- SQLite memory
- Simple project indexing
- Git integration via installed git
- Python/C++ execution
- Optional local Ollama support if installed
- Plugin system
